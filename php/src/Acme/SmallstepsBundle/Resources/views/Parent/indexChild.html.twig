    {% extends 'AcmeSmallstepsBundle::parentBase.html.twig' %}

{% block heading %}
    Child list
{% endblock heading %}

    {% block content -%}


    <div class="table-responsive">
        <table class="table table-bordered table-responsive table-striped">
            <thead>
                <tr>
                    <th>Firstname</th>
                    <th>Lastname</th>
                    <th>Dob</th>
                    <th>Nhsno</th>
                    <th>School</th>
                    <th>Room</th>
                    <th>Additional</th>                  
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('edit_child', { 'id': entity.id }) }}">{{ entity.firstname }}</a></td>
                    <td>{{ entity.lastname }}</td>
                    <td>{% if entity.dob %}{{ entity.dob|date('d-m-Y') }}{% endif %}</td>
                    <td>{{ entity.nhsNo }}</td>
                    <td>{{ entity.school.name }}</td>   
                     <td>{{ entity.room.name }}</td> 
                    <td>{{ entity.additional }}</td>                    
                    <td>
<ul>
    <li>
        <a href="{{ path('edit_child',
                     { 'id': entity.id }) }}" >edit</a>
    </li>
</ul>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

            <ul>
            <li>
                <a href="{{ path('add_child') }}">
                    Create a new entry
                </a>
            </li>
        </ul>
        {% endblock %}
